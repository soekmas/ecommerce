# Server Config
PORT=8080
APP_ENV=development

# Database Config
DB_HOST=localhost
DB_PORT=5432
DB_USER=root
DB_PASSWORD=secretpassword
DB_NAME=ecommerce

# Redis Config
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# JWT Config
JWT_SECRET=supersecretkey_change_in_production
JWT_EXPIRE_HOURS=24

# Biteship Logistics API
BITESHIP_API_KEY=biteship_staging_key_here

# SMTP Server Config (Mailtrap/Mailhog for dev)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply@go-commerce.local
